# Database Configuration for GlobalBooks Catalog Service
# Location: src/main/resources/database.properties

# PostgreSQL Database Connection
db.driver=org.postgresql.Driver
db.url=jdbc:postgresql://localhost:5432/globalbooks_catalog
db.username=postgres
db.password=20010219

# Connection Pool Settings
db.pool.size=10
db.pool.min=5
db.pool.timeout=20000

# Optional: SSL Configuration (uncomment if needed)
# db.ssl=false
# db.sslfactory=org.postgresql.ssl.NonValidatingFactory

# Optional: Advanced PostgreSQL Settings
# db.prepareThreshold=5
# db.connectTimeout=10
# db.socketTimeout=30